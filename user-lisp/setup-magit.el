;; Hace que Magit pueda modificar los buffers al hacer cambios en el repo
(setq magit-revert-buffers t)
;; Para que no nos muestre un mensaje al inicio
(setq magit-last-seen-setup-instructions "1.4.0")

(provide 'setup-magit)
